# https://github.com/cheat/cheatsheets
- name: iconv
  keybinds:
  - name: "To convert file (iconv.src) from iso-8859-1 to utf-8 and save to /tmp/iconv.out"
    key: "iconv -f iso-8859-1 -t utf-8 iconv.src -o /tmp/iconv.out"
