# https://github.com/cheat/cheatsheets
- name: install
  keybinds:
  - name: "To create a directory for a user with a group with permission 700"
    key: "install -o <username> -g <groupname> -m 700 -d /home/<username>/<directoryname>"
